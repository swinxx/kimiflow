<!-- kimiflow:phase-detail source=docs/render/kimiflow/canonical/SKILL.md -->

## 🟢 Phase 7 — Code-review ensemble against specs → fix → commit-gate

1. **Review packet.** Build one compact packet for reviewers: changed-file list, `git diff --stat`, relevant diff hunks, `ACCEPTANCE.md`, `INTENT.md`/`PROBLEM.md`, `DIAGNOSIS.md` when present, `BUG-REPRO.md` when present, decisive Phase-6 evidence, and the exact verification commands. For large/high-risk handoffs or background/context reuse, generate a bounded `hooks/agentic-readiness.sh packet --run .kimiflow/<slug> --kind review --write` packet and record the readiness status.
2. **Candidate lenses in parallel.** Read → reference.md "Review rubric" (orchestrator only). Spawn narrow fresh-context reviewers with adversarial framing; **reviewers do not read reference.md** — inline each spawn's lens line + the CANDIDATE grammar + file-form constraints (column 0, no newline in the claim; nothing but CANDIDATE lines or the single sentinel `NONE`) into the prompt. `small` runs use at least **two** lenses (`quick` alias: review light — ONE lens, `bug-regression`); add the third when the diff touches hooks, plugins, memory, launcher, APIs/contracts, multiple surfaces, or any high-risk path. `large`/release-critical uses all three. One lens (default `bug-regression`) is **cross-family by default** when available — same transport/fallback as Phase 4 (→ reference.md "Model routing (per-role)"). The standard lenses are:
   - **R1 bug-regression:** logic, edge cases, acceptance drift, weakened/insufficient tests.
   - **R2 failure-security:** input validation, secrets/privacy, path handling, rollback/failure atomicity, partial writes.
   - **R3 integration-contract:** installed hooks/metadata/docs/launcher wiring, host parity (Claude/Codex), command/API/schema contracts.
   Each lens writes `.kimiflow/<slug>/code-review-candidates/r<N>-<lens>.md` using `CANDIDATE <SEVERITY> <ref> :: <claim> :: verify=<smallest check>` or `NONE`.
3. **Orchestrator verification.** Verify each candidate with targeted code reads, commands, or reproduction; for BLOCKER/HIGH candidates include an **active refutation attempt** (execute its `verify=` check, read the full code path — "could this be wrong?"): refuted → rejected, not promoted. Write `CODE-REVIEW.md` with all candidates, verification evidence, duplicates merged, rejected/unverified candidates, and confirmed findings. Only confirmed BLOCKER/HIGH/MEDIUM/LOW lines are promoted to `.kimiflow/<slug>/findings/r<N>-code-verified.md` in the canonical `FINDING <SEVERITY> <ref> :: <reason>` format; if none are confirmed, write `NONE`. The resolver gate counts only this promoted file, never raw candidates.
4. **Advisories.** First stage the run's named changed paths (`git add <named paths>` — never `-A`; the scans read the staged diff). Then run the bundled test-weakening scan `${CLAUDE_PLUGIN_ROOT:-$CLAUDE_SKILL_DIR}/hooks/test-weakening-scan.sh` over the staged diff → append its `FLAG` lines to `.kimiflow/<slug>/ADVISORIES.md`; if it can't be located or run, note it in STATE — never silently skip the advisory channel. Likewise run `hooks/secret-content-scan.sh` and `hooks/lsp-diagnostics.sh` when present. The Simplicity dimension still writes `FLAG`s to advisories (folded into an existing lens for `small`; dedicated prosecutor for `large` or size-tripwire).
5. **Fix.** Fix confirmed BLOCKER/HIGH, then re-run the candidate lenses affected by the fix and repeat verification. Resolve with `${CLAUDE_PLUGIN_ROOT:-$CLAUDE_SKILL_DIR}/hooks/resolve-review-gate.sh .kimiflow/<slug>/findings --round <N> --expect code-verified`. Same fail-closed, blocker-aware anti-oscillation and cap rules as Phase 4.
6. **Commit-gate — STOP.** Read → reference.md "Commit hygiene". **Advisory triage (fail-closed):** present every `.kimiflow/<slug>/ADVISORIES.md` `FLAG`; the commit is blocked until each is explicitly dismissed with a reason (legit refactor) or promoted (→ a real `FINDING HIGH` → back to phase-7 review). Then show: short summary, `git status`, `git diff --staged`. **Wait for explicit OK.** Then commit (only named paths, no `git add -A`, no co-author/AI trailer, tests green). **(large)** if `.kimiflow/test-gate` doesn't exist, write it with the test command verified green in phase 6 (idempotent, kept local/untracked — never staged or committed; suggest gitignoring `.kimiflow/`) and announce it. **(audit)** execute one slice at a time: verify its repo-wide grep returns 0 (A3), apply the cut/shrink, run the slice's verify gate (grep-sweep → typecheck/build → tests green; `shrink`/`stdlib` green before+after), edit companion tests in lockstep, then commit **one slice = one reviewable diff = one commit**. Never batch slices into one commit.
7. **Learning loop + project memory — required before `Status: done`.** Run `MR review-run --run .kimiflow/<slug> --write` after verification/review — it records the learning set in `.kimiflow/project/LEARNINGS.jsonl`, refreshes bounded memory/index/metrics/proposal/provider state, and returns a compact notification. `review-run` is **quality-gated**: too-short/generic learnings, missing evidence, or unsafe memory content block the write. If the run has no durable learning, use `MR review-run --run .kimiflow/<slug> --write --skip "<reason>"` so the skip is explicit. Then run `MR verify-run --run .kimiflow/<slug>`; a CLOSED result blocks completion, including stale/changed evidence fingerprints. User/workflow preferences go through `record --scope user` (local-only). Provider/Vault safety: neither `.kimiflow/` nor repo files store the key; prefetch/sync handoffs export only freshly verified current, non-private, non-security learnings; it never patches skills or writes external notes blindly — approved skill candidates become review-only drafts under `.kimiflow/project/SKILL-DRAFTS/`. Everything else (history search, usage/economics/usefulness metrics, `consolidate`/`propose` curation, the provider subcommands): → reference.md "Memory Router & Learning Loop".
8. **Project-map auto-refresh** (recommended, non-blocking). After Phase-6 verify, run `PMS refresh --changed` (`PMS` = `${CLAUDE_PLUGIN_ROOT:-$CLAUDE_SKILL_DIR}/hooks/project-map-status.sh`; Codex: `KIMIFLOW_HOST=codex` + `KIMIFLOW_PLUGIN_ROOT`) so the sections of the files this run touched are re-hashed back to `current`, deleted members are pruned, new files under a known section prefix are adopted (+sha256), and each refreshed `.sh` section's symbol index is rebuilt. `NEW-FILE` lines are structure hints only — no auto-facts; durable learnings still flow through the gated learning loop above. Skip silently when there is no `.kimiflow/project/INDEX.json`. The Stop-hook map-staleness nudge (`hooks/map-staleness-nudge.sh`) is the safety net that surfaces any residual staleness. → reference.md "Project Map Bootstrap".
8a. **Workqueue close-back** (recommended, non-blocking). If this run implemented a slice from the local workqueue (`.kimiflow/project/IMPROVEMENTS.md` `## Priorisierte Slices` or `.kimiflow/project/FINDINGS.md` `## Offen`), mark it done so the launcher stops counting it open: `${CLAUDE_PLUGIN_ROOT:-$CLAUDE_SKILL_DIR}/hooks/improvements-status.sh list [--queue improvements|findings]` to find the stable id, then `improvements-status.sh mark-done <id> --commit <sha> --write` (Codex: `KIMIFLOW_HOST=codex` + `KIMIFLOW_PLUGIN_ROOT`). Attribution is EXPLICIT — only mark a slice the run actually closed; never auto-guess. The marker is in-place and `reopen <id>` is the inverse. The Stop-hook `hooks/improvements-staleness-nudge.sh` is the non-blocking backstop: it surfaces a once-per-day nudge only when a run just completed while open slices remain. Skip silently when no workqueue file exists. → reference.md "Workqueue close-back".
9. **Close state.** For phase-read runs, read/record the Phase 7 file before the final `hooks/active-run.sh finish --write`; finish checks fresh reads through Phase 7. Only after the commit gate and learning review are open, set all phases done and set `Status: done` in `STATE.md`.
